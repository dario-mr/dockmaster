apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dockmaster-production
  namespace: kube-system
spec:
  secretName: dockmaster-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - ${DOMAIN}
