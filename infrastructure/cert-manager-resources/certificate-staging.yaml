apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dockmaster-staging
  namespace: kube-system
spec:
  secretName: dockmaster-tls-staging
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging
  dnsNames:
    - ${DOMAIN}
