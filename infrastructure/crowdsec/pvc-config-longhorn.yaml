apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crowdsec-config-pvc-longhorn
  namespace: crowdsec
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
